name: align
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch
      - torchaudio
      - transformers
      - uroman
      - psycopg2-binary
      - requests
      - soundfile
